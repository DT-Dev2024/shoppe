export enum path {
  home = "/",
  user = "/user",
  orderHistory = "/user/order-history",
  profile = "/user/profile",
  login = "/login",
  logout = "/logout",
  productDetail = "/productDetails",
  cart = "/cart",
  checkout = "/checkout",
}

export enum path {
  home = "/",
  user = "/user",
  profile = "/user/profile",
  login = "/login",
  logout = "/logout",
  productDetail = "/productDetails",
  cart = "/cart",
  checkout = "/checkout",
}

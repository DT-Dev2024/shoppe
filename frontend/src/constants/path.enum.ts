export enum path {
  home = "/",
  orderHistory = "/order-history",
  login = "/login",
  logout = "/logout",
  productDetail = "/productDetails",
  cart = "/cart",
  checkout = "/checkout",
}
